		{% extends 'layout.html.twig' %}

		{%   block content  %}

        <h1>{{title}}</h1> 
         <h3> Bonjour je suis dans l'inscription</h3>


         <table class="table" border="1">
                <thead class="thead-dark">
                    <tr>
                        <th>Prenom</th>
                        <th>Pseudo</th>
                        <th>Nom</th>
                        <th>email</th>
                        <th colspan="2">Actions</th>
                    </tr>
                </thead>
            {% for membre in membres %}
            <tr>
               
                <td>{{ membre.prenom }}</td>
                <td>{{ membre.username }}</td>
                <td>{{ membre.nom }}</td>
                <td>{{ membre.email }}</td>
                <td><a href="{{ path('update_membre', { 'id' : membre.id_membre } ) }}"><i class="far fa-edit"></i></a></td>
			    <td><a href="{{ path('delete_membre', { 'id' : membre.id_membre } ) }}"><i class="far fa-trash-alt"></i></a></td>

            </tr>
            {% endfor %}
            </table>



        {% endblock %}