{% extends 'layout_admin.html.twig' %}

{% block content %}

<img src="{{ asset('/photo/' ~ photo)}}" width="50"/>

{{form(produitForm)}}





	<!-- 4 suite : ajout du JS pour afficher le popup panier : -->
		<!-- <script>
			$(function(){
				// Show the Modal on load
				$("#myModal").modal("show");
			});
		</script> -->
 
{% endblock %}